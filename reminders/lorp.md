<!-- LORP reminders — ONE PER LINE. Each non-comment line prints as its own reminder in the LORP section.
     Turn a single line off by putting # at the start of it. Anything inside <!-- --> is ignored.
     Empty (or only comments) = no reminders. Write your reminders below this block, one per line. -->
Check the 4H chart potential MACD0 changes as well as any supply/demand zones during a trade!
Recent losses have been as a result of hitting Supply Zones!!!!
CHECK Bar Prediction Values!
Compare tonight's SBT CSV exports against tomorrow's brief results (same-day this time, not the Sep 3-4 vs Sep 8 mismatch from 9 Sep) — which scans actually match today's real LORP/SID entries.
INVESTIGATE (9 Sep 2026): sync-watchlist.cjs excluded 8 of 13 tickers written to "Brief Output" as "ASX/invalid" (MLYS, PBR, RHI, RNG, SENEA, SHG, SPT, ZETA) even though none are actually ASX-listed — looks like a symbol-format mismatch between what push-watchlist.cjs writes and what sync-watchlist.cjs recognizes as valid, not a real market-listing issue. PRIORITY: several of the excluded tickers (MLYS, PBR, RHI, SHG) were today's genuine LORP Pullback/reversion candidates — David's original intent was specifically to have LORP Reversion tickers reliably committed to this watchlist, so this bug is undermining that directly. Fix the mismatch, don't just reclassify.
PRIORITY (10 Sep 2026): the new LORP Pullback Watch (5-day persistence, built tonight) is currently non-functional — "LORP SCREENER" watchlist section is empty (0 members), so the evict-on-drop check removes every tracked ticker on the same run it's added. Need to find out from David how "LORP SCREENER" is meant to be repopulated (his own TV Screener export? another script?) and get it populated again — the watch logic itself is correct and tested, it just has nothing to check membership against right now.
