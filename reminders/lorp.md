<!-- LORP reminders — ONE PER LINE. Each non-comment line prints as its own reminder in the LORP section.
     Turn a single line off by putting # at the start of it. Anything inside <!-- --> is ignored.
     Empty (or only comments) = no reminders. Write your reminders below this block, one per line. -->
Check the 4H chart potential MACD0 changes as well as any supply/demand zones during a trade!
Recent losses have been as a result of hitting Supply Zones!!!!
CHECK Bar Prediction Values!
Compare tonight's SBT CSV exports against tomorrow's brief results (same-day this time, not the Sep 3-4 vs Sep 8 mismatch from 9 Sep) — which scans actually match today's real LORP/SID entries. Files ready at ~/Downloads/SBT_Latest/ (8 files, confirmed 10 Sep 2026: 1,2,3PullbackBullish/MACDBullishSignalLineCross/New52WeekClosingHigh/combo-scan all _LORP, plus combo-scan/rsi_cross _SID and _SID2). Compare against tomorrow's brief-*-lorp.json and -sid.json once the 7am run completes.
RESOLVED (13 Sep 2026): the 9 Sep "ASX/invalid" exclusion bug (MLYS, PBR, RHI, RNG, SENEA, SHG, SPT, ZETA) is fixed — analyse-brief.cjs's watchlist-updates.json sidecar was writing bare tickers with no exchange prefix, so push-watchlist.cjs's resolveExchangePrefix had nothing to borrow a prefix from for tickers new to the watchlist, and sync-watchlist.cjs then rejected the resulting bare symbol as invalid. Now writes fully-qualified EXCHANGE:TICKER symbols straight from today's scan data. Watch the next few briefs to confirm no more "ASX/invalid" exclusions on genuinely new tickers.
PRIORITY (10 Sep 2026): the new LORP Pullback Watch (5-day persistence, built tonight) tested with an empty "LORP SCREENER" section, causing every tracked ticker to be evicted same-run — RESOLVED, this is expected/normal: David manually updates LORP/SID/SBT screener sections from TV/SBT CSVs every night before 11pm AEST, and testing happened before that window. Re-verify the watch feature works correctly on a fresh morning brief after his nightly update has run.
